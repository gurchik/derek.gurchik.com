{% extends "_base.html.j2" %}

{% block body %}
{{ content }}

{#
<h3>Posts</h3>
<ol id="posts-list">
  {% for post in posts %}
  <li>
    <span class="post-date">{{ post["date"].strftime('%b %e, %Y') }}</span>
    <a href="{{ post["url"] }}">{{ post["title"] }}</a>
  </li>
  {% endfor %}
</ol>
#}

{% endblock %}